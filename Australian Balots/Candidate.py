class candidate:
  def _init_(self, name):
    self.name = name
    self.count = 0
  
  def increaseCount(self)
    self.count +=1
    
  def getCount(self)
    return count
    
  def getName(self)
    return self.name
  
  def print(self)
    print(self.name + " " + self.count)
